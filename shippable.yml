language: python

python:
  - 2.7.12 
  - 3.7.0 
  - pypy #5.10.0
  - pypy3 #5.10.1
 
build:
  pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: master
    pull: true
    
  ci:
    - python --version 
    - pip --version #8.1.2
    - virtualenv --version #15.1.0
    - java -version
