language: python

python:
  - 2.7  
  - 3.7  
  - pypy2 
  - pypy3 
 
build:
  pre_ci_boot:
    image_name: drydock/u16pytall
    image_tag: master
    pull: true
    
  ci:
    - python --version 
    - pip --version 
    - virtualenv --version
    - java -version
